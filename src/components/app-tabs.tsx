

export default function AppTabs() {
  // Intentionally hidden: your app uses AppNavigator + StudentTabBar,
  // so the default expo-router native tabs would show an extra Home/Explore bar.
  return null;
}

