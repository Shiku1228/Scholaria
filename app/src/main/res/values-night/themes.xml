<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Scholaria" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/login_button_bg</item>
        <item name="colorOnPrimary">@color/login_button_text</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/login_bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>